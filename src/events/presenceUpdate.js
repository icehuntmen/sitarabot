module.exports = async (unite, oldMember, newMember) => {

    /*
    if (oldMember.status === undefined) { devs.warn("Missing message for oldMember.status"); return  } else { oldMember.status = null };
    if (newMember.status === undefined) { devs.warn("Missing message for newMember.status"); return } else { newMember.status = null };

    const colorRed = clc.red.bold;
    const colorGreen = clc.magentaBright.bold;

   // devs.info(`PresenceUpdate: ${colorGreen(unite.users.cache.get(newMember.userID).tag)} on guild ${unite.guilds.cache.get(newMember.guild.id).name} change status from ${colorRed(oldMember.status)} to ${colorGreen(newMember.status)}`)
    devs.info(`PresenceActivities: ${colorGreen(unite.users.cache.get(newMember.userID).tag)} change activities ${colorRed(newMember.activities)}`)

   //devs.showObject(newMember)
    /*devs.showObject(oldMember,1)
    devs.showObject(newMember,1)*/

};