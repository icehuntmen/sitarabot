module.exports = async (unite, oldMessage,newMessage) => {

    devs.info('messageUpdate events!')
   /* devs.showObject(oldMessage,0)
    devs.showObject(newMessage,0)
    devs.info('messageUpdate events end!')*/

};